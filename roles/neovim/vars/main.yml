neovim_build_dependencies: {
  Debian: [
    autoconf,
    automake,
    cmake,
    curl,
    doxygen,
    gettext,
    g++,
    git,
    libtool,
    libtool-bin,
    ninja-build,
    pkg-config,
    unzip
  ],
  Archlinux: [
    base-devel,
    cmake,
    curl,
    git,
    ninja,
    tree-sitter,
    unzip
  ],
  Fedora: [
    autoconf,
    automake,
    cmake,
    curl,
    gcc,
    gcc-c++,
    gettext,
    git,
    libtool,
    make,
    ninja-build,
    patch,
    pkgconfig,
    unzip
  ]
  }
